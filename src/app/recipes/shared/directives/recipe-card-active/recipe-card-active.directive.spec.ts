import { RecipeCardActiveDirective } from './recipe-card-active.directive';


describe('RecipeCardActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RecipeCardActiveDirective();
    expect(directive).toBeTruthy();
  });
});
